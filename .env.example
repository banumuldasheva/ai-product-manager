# Local dev: leave DATABASE_URL unset → SQLite is used automatically (no install needed).
# On Render: DATABASE_URL is injected from the database resource — do not set it here.
# DATABASE_URL=

PORT=3001
NODE_ENV=development
